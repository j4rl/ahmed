<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<?php
/*     require_once("crypt.php");
    $x=new Database("case");
    $y=new Database("case");
    $result=$x->runQuery("SELECT * FROM tblMember");
     while($rad=mysqli_fetch_assoc($result)){
         echo $rad['namn']."<br>";
         $memberID=$rad['memberID'];
         $name=$x->enc($rad['namn']);
         $medlnr=$rad['medlnr'];
         $email=$x->enc($rad['email']);
         $telefon=$x->enc($rad['telefon']);
         $medlemsform=$x->enc($rad['medlemsform']);
         $skolform=$x->enc($rad['skolform']);
         $arbetsplats=$x->enc($rad['arbetsplats']);
         $arbetsgivare=$x->enc($rad['arbetsgivare']);
         $befattning=$x->enc($rad['befattning']);
         $sql="INSERT INTO tblmem VALUES ($memberID,'$name', '$medlnr', '$email', '$telefon', '$medlemsform', '$skolform', '$arbetsplats', '$arbetsgivare', '$befattning' )";
         $res=$y->runQuery($sql) or die("Nope.");
        // echo $sql."<br>";
     }

    $x->close();
    $y->close(); */
?>    
</body>
</html>


